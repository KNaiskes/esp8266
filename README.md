esp8266 microcontroller projects

## Setup development enviroment (for Arduino framework)

### Create and activate Python virtual enviroment
```
$ python -m venv venv
$ source venv/bin/activate
$ pip install -r requirements.txt
```
